export class Customer {
    // TODO: add ? after field names if are optional.
    constructor(id, name, password, coupons) {
        this.id = id;
        this.name = name;
        this.password = password;
        this.coupons = coupons;
    }
}
//# sourceMappingURL=customer.js.map